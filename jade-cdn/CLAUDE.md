# 静态工具库

## 有效的内容

当前这个项目目录中，有效的内容只有`cdn-libs/3rd/`和`cdn-libs/jadeutils.v3/`这两个目录是有效的目录。其他的目录都是无效的临时文件，忽略他们。

项目结构：

- `cdn-libs/3rd/`目录：这里是第三方的库。
- `cdn-libs/jadeutils.v3/`目录：这里是我们的工程目录。
- `cdn-libs/jadeutils.v3/CLAUDE.me`文件：这里是环境上下文。


